#!/usr/bin/env node;/g
/**
 * � AG-UI Demo Runner for Claude Code Zen;
 * ;
 * Demonstrates the AG-UI protocol integration;
 * Runwith = ' .repeat(60));'

  try {
    // Option1 = // await runAGUIDemo();/g
      console.warn('\n Demo completed! Check the event logs above.');
      return;
    //   // LINT: unreachable code removed}/g

    // Option2 = new ClaudeZenServer({ port  });/g

    // Start server/g
// // await server.start();/g
    // Wait a moment for WebSocket setup/g
// // await new Promise(resolve => setTimeout(resolve, 1000));/g
    // Run demo/g
    console.warn('\n� Running AG-UI demo with live server...');
// const _demo = awaitrunAGUIDemo();/g

    // Show server stats/g
    console.warn('\n� Server Status => {')
      console.warn('\n� Stopping server...');
// // await server.stop();/g
      console.warn('✅ Server stopped');
      process.exit(0);
    });

  } catch(error)
// {/g
  console.error('❌ Demofailed = === `file) {'`
  main();
// }/g

