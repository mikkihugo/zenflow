/**
 * Shared Vision API - Works for both TUI and Web interfaces
 */

export class VisionAPI {
  constructor(baseUrl = 'http = baseUrl;
}

async;
fetchVisions();
{
    try {
      const response = await fetch(`${this.baseUrl}/api/v1/visions`);
      if (!response.ok) throw new Error(`HTTP ${response.status}`);
      return await response.json();
    } catch(_error) {
      // Fallback to mock data for development
      console.warn('Using mock vision data = {}): any {
    try {
      const _response = await fetch(`${this.baseUrl}/api/v1/visions/${visionId}/roadmap/generate`, {method = new VisionAPI();
