/**
 * Deprecated duplicate: this path now re-exports the canonical implementation.
 * Keep import stability for existing consumers while ensuring a single source of truth.
 */
export * from '../../../infrastructure/websocket/socket.manager';
