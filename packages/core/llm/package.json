{
  "name": "@claude-zen/llm",
  "version": "1.0.0",
  "description": "LLM providers and integration for Claude Code Zen",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json --skipLibCheck",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --cache --max-warnings=0",
    "test": "vitest run"
  },
  "dependencies": {
    "@claude-zen/foundation": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
