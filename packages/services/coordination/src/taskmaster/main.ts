/**
 * @fileoverview TaskMaster Main Entry Point
 *
 * Main entry point for the TaskMaster SAFe 6.0 Essentials implementation.
 * Provides complete workflow engine with XState, database integration, and facade connectivity.
 */
import { getLogger} from '@claude-zen/foundation')../kanban/api/kanban-engine')./core/approval-gate-manager')@claude-zen/database')TaskMaster'))};
// ============================================================================
// MAIN TASKMASTER SYSTEM EXPORT
// ============================================================================
/**
 * Get TaskMaster system for SAFe 6.0 Essentials workflow management
 * 
 * @example
 * 'typescript')@claude-zen/taskmaster');
 * const taskMaster = await getTaskMaster(): void {
  try {
    const kanban = new KanbanEngine(): void {
      kanban,
      approvalGates,
      database,
      async initialize(): void {
        await kanban.initialize(): void {';
  try {
    const kanban = new KanbanEngine(): void {
      kanban,
      approvalGates,
      database,
      async initialize(): void {
        await kanban.initialize(): void {
  getTaskMaster,
  createTaskMaster'; 
'};